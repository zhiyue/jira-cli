# dist

Build artifacts and release helpers. This directory is populated by the release
workflow (`.github/workflows/release.yml`). You typically don't commit anything
here by hand.

Subdirectories:
- `homebrew/` — Homebrew formula template (created in a later commit).
